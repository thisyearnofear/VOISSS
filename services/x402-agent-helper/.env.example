# X402 Agent Helper V2 Environment Configuration
# 
# NO CENTRAL WALLET NEEDED - Agents provide their own keys!
# This maintains the permissionless nature of x402.

# Optional: Network to use (base for mainnet, base-sepolia for testing)
NETWORK=base

# Optional: VOISSS API URL
VOISSS_API_URL=https://voisss.netlify.app

# Optional: Server port
PORT=3001

# NOTE: No AGENT_WALLET_PRIVATE_KEY needed!
# Each agent provides their own key when calling the API.
# This keeps x402 truly permissionless - no central funding required.

/**
 * Public surface of the VOISSS API registry.
 *
 * See `./routes.ts` for the canonical route definitions.
 * This barrel exists so callers can `import { ROUTES, findRoute } from '@voisss/shared/api'`.
 */

export * from './routes';
